
Program ex05;

Var angulo : real;

Function seno (a, b :LongInt): LongInt;

Var 
Begin

End;

Function coss (a, b :LongInt): LongInt;

Var 
Begin

End;

Function tang (a, b :LongInt): LongInt;

Var 
Begin

End;

Begin

  read (angulo);
  If tang(angulo, tg) Then
    writeln (tg:0:5)
  Else
    writeln ('nao existe tangente de ',angulo:0:5);

End.
